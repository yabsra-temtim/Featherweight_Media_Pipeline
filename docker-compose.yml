services:
  postgres:
    image: postgres:16-alpine
    container_name: featherweight-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: featherweight
      POSTGRES_PASSWORD: featherweight
      POSTGRES_DB: featherweight
    ports:
      - "5432:5432"
    volumes:
      - featherweight-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U featherweight -d featherweight"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  featherweight-postgres-data:
