# ── Local development only ───────────────────────────────────────────────────
# On Render, VITE_API_URL is set in the dashboard to the backend service URL.
VITE_API_URL=http://localhost:8080
VITE_MAX_UPLOAD_MB=32
