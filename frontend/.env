# ── Local development only ───────────────────────────────────────────────────
# On Render, VITE_API_BASE_URL is injected automatically at build time
# via the fromService env var in render.yaml — do NOT hardcode the
# production URL here.
VITE_API_BASE_URL=http://localhost:8080
VITE_MAX_UPLOAD_MB=32
