 # URL of the Featherweight Go backend (see /backend).
VITE_API_URL=http://localhost:8080

# Client-side max upload size shown in validation messages (MB).
# Keep this at or below the backend's MAX_UPLOAD_SIZE_MB.
VITE_MAX_UPLOAD_MB=10
